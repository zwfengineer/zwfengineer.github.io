function hvmenu() {
    var menu = document.getElementsByClassName('menu')[0];
    var menuico = document.getElementsByClassName('menuico')[0];
    menu.hidden = (menu.hidden) ? false:true;
    menuico.hidden = (menuico.hidden) ? false:true;
}